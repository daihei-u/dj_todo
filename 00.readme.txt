Django templateの限界

確かにテンプレートは便利である。

テンプレートから、ベースのテンプレートを呼び出せる。
テンプレートに別のテンプレートを埋め込める
テンプレートでクラスの関数を扱える。
制御文が使える。
  制御文の十分条件とはあるのか？
    繰り返しと分岐とスタックされる関数があればすべての制御は記述できるのだろうか？
    そもそもすべての制御とは？制御は有限なのか？構造化プログラミングでそのあたりは証明されているのだろうか？
    制御
      if/elif/else
      For
      While
      break
      goto(関数含む)
    この制御に縛られている限り完全なる並列処理は難しいのではないか？
